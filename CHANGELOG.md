2026-08-25 14:10 - Implemented config-dir-scoped OS credential store lookup, fixing `--config-dir`/`CLAUDE_CONFIG_DIR` for real macOS/Windows profiles. New `src/adapters/credentials/keychain-scope.ts` exports `keychainServiceName(configDir?)`, deriving the entry name Claude Code itself scopes to a config dir — `Claude Code-credentials` for the default profile, `Claude Code-credentials-<hash>` otherwise, where `<hash>` is the first 8 hex chars of `sha256(configDir)` (verified against two real Keychain entries). `readFromKeychain`/`readFromWindowsCredentialManager` (`src/adapters/credentials/{keychain-reader,wincred-reader}.ts`) now take an optional `configDir` and read that scoped entry; the PowerShell script builder is parameterized on the target instead of using a module-level constant. `KeychainCredentialsProvider.readFromConfigDir` (`src/adapters/keychain-credentials.ts`) now runs the same store-then-file order scoped to the given directory — on darwin/win32 the scoped OS store entry is tried first, then `<configDir>/.credentials.json` — instead of skipping the OS store outright; the "never leak another account's token" guarantee is kept by only ever reading the entry/file scoped to the requested directory, never the unscoped default or another dir's file. Updated `CredentialsOptions` docs (`src/core/types.ts`), the README's "Choosing the config directory" section, and `.docs/guidelines/credentials.md`/`testing.md` to match. Verified live end to end: `node dist/cli.js --config-dir /Users/inteligenti/.claude-uniques --json` now succeeds with real usage data (was `CREDENTIALS_NOT_FOUND`); `--config-dir /Users/inteligenti/.claude` now reaches the scoped entry too (fails differently, on an expired token — `AUTHENTICATION_FAILED`, proving the credential was found); the README's illustrative `--config-dir ~/work/.claude` still fails cleanly with `CREDENTIALS_NOT_FOUND` since that profile was never logged into on this machine. Added `tests/adapters/credentials/keychain-scope.spec.ts` (pinned against independent SHA-256 test vectors, not the implementation) and extended `keychain-reader.spec.ts`, `wincred-reader.spec.ts`, `tests/adapters/keychain-credentials.spec.ts` for the scoped-then-file order; suite green at 21 files / 178 tests (up from 19/164), `npx tsc --noEmit`, `npm run lint`, and `npm run build` all exit 0. Removed the now-resolved `.docs/ai-backlog/2026-08-25-config-dir-macos-keychain-gap.md`.

2026-08-18 13:33:13 - Purged the leaked npm access token from git history. `remove.npmrc` (containing a live `//registry.npmjs.org/:_authToken=`) had been committed in b776bc0 and was present in every one of the 40 unpushed commits, so GitHub push protection (GH013) rejected the push. Rewrote b776bc0^..HEAD with `git filter-branch --index-filter "git rm --cached --ignore-unmatch remove.npmrc"`, keeping a safety branch `backup-before-secret-removal` at the old 0120ef2. Hardened `.gitignore` with `*.npmrc` so any variant filename is ignored, not just `.npmrc`. The token itself still needs revoking on npmjs.com.

2026-08-17 16:31:22 - Added a real-filesystem integration spec (tests/adapters/credentials/config-dir.integration.spec.ts) locking that credentials are actually read from CLAUDE_CONFIG_DIR / the configDir option, with the OS credential store bypassed.

2026-08-17 16:24 - Validated step 1 of `claudeconfigdir-custom` (configurable Claude config directory) end to end against the built `dist/`, and cleared the lint gate. Executable proof: a Node script drove the compiled `KeychainCredentialsProvider`/`resolveConfigDir`/`readFromFile` on darwin against real temp config dirs — 17/17 checks passed (env `CLAUDE_CONFIG_DIR` honoured with the Keychain bypassed, constructor option beating the env var, `~/` expansion reading a real home directory, a missing dir and a `.credentials.json` without `claudeAiOauth` both raising `CredentialsNotFoundError` naming the resolved path, the full precedence/trim/blank table, and the no-argument `readFromFile()`/`new KeychainCredentialsProvider()` backward-compatibility paths). The real binary was also driven: `CLAUDE_CONFIG_DIR=<dir> node dist/cli.js` reached the API with the fixture token and returned `Error [AUTHENTICATION_FAILED]` (HTTP 401 — proof the token came from the config dir, not the Keychain), and a missing dir printed `Error [CREDENTIALS_NOT_FOUND]` naming the path, with no placeholder or empty values anywhere. `npx tsc --noEmit`, `npm test` (19 files / 164 tests) and `npm run build` all exit 0 and `dist/adapters/credentials/config-dir.js` exists. The `npm run lint` acceptance criterion was failing on 12 pre-existing errors, so they were fixed here: `void` on the floating promise in `src/cli.ts`, redundant type assertions dropped in `tests/adapters/anthropic-usage.spec.ts` and `tests/adapters/credentials/file-reader.spec.ts`, `async` test doubles turned into `Promise.resolve` in `tests/core/app.spec.ts`, the `JSON.parse` callback given a block body in `tests/cli.entry.spec.ts`, and justified inline disables for the ANSI `\x1b` regexes (`tests/cli.spec.ts`, `tests/presentation/console-renderer.thresholds.spec.ts`) and the BOM fixture (`tests/adapters/credentials/parse.spec.ts`). `npm run lint` now exits 0; the resolved backlog item `.docs/ai-backlog/2026-08-16_11-20-48-test-files-fail-lint.md` was removed and a new one filed for the missing `--help`/`--version` flags.

2026-08-17 15:53 - Tested step 2 of `claudeconfigdir-custom` (`--config-dir` flag and `getUsage(options?)`). The unit seams were already covered, but nothing exercised the flag at the level a user actually drives it, so `tests/e2e/cli.e2e.spec.ts` was extended: `runCli` now takes CLI arguments, and a new `compiled CLI with an explicit config directory` block runs the real `dist/cli.js` as a subprocess (offline, fake HOME, PATH-shadowed credential-store stubs) asserting that `--config-dir <path>` and `--config-dir=<path>` (composed with `--json`) each exit 1 and echo the resolved directory back in the `Error [CREDENTIALS_NOT_FOUND]` message rather than a placeholder, that `--config-dir` outranks `CLAUDE_CONFIG_DIR` (the flag directory appears, the env one does not), that `CLAUDE_CONFIG_DIR` alone is still honoured, and that `--config-dir --json` does not swallow `--json` as the path but falls back to default resolution. Mutation-checked by reverting `run()` to `new KeychainCredentialsProvider()`: 3 of the new cases fail, so the guard bites on regression; the source was restored and rebuilt. Suite green at 19 files / 164 tests (up from 159), no skips; `npx tsc --noEmit` and `npm run build` exit 0. `npm run lint` still reports exactly the same 12 pre-existing `tests/` errors (already filed as `.docs/ai-backlog/2026-08-16_11-20-48-test-files-fail-lint.md`), none in the e2e spec. No production code and no other test file changed.

2026-08-17 14:19 - `--config-dir` exposed on both entry points (step 2 of `claudeconfigdir-custom`). `src/cli.ts` gains a pure `selectConfigDir(argv)` seam next to `selectRenderer`: it accepts `--config-dir <path>` and `--config-dir=<path>`, the last occurrence wins, and it returns `undefined` — never throwing — when the flag is absent, when it is the final argument, or when the next token starts with `--`, so `--config-dir --json` does not swallow `--json`; `run(argv)` now builds `new KeychainCredentialsProvider({ configDir: selectConfigDir(argv) })`, composing with `--json` in any argument order and leaving existing invocations and exit codes unchanged. `src/index.ts` widens the facade to `getUsage(options?: CredentialsOptions)`, forwarding the option to the provider (`getUsage()` with no argument behaves exactly as before) and re-exporting the `CredentialsOptions` type. Docs: `README.md` gains a "Choosing the config directory" section stating the `--config-dir` > `CLAUDE_CONFIG_DIR` > default (`~/.claude`, `%APPDATA%\.claude` on Windows) precedence, that an explicit config dir reads only `<dir>/.credentials.json` and skips the OS credential stores, and that `~/` is expanded, with one CLI and one library example; `.docs/guidelines/entry-points.md` documents `selectConfigDir` under the CLI seams and the new `getUsage(options?)` signature under the library facade. Tests: 6 `selectConfigDir` cases plus 2 `run` constructor-argument cases in `tests/cli.spec.ts` and 2 `getUsage` forwarding cases in `tests/index.spec.ts` (both adapter mocks now record their constructor options); suite green at 19 files / 159 tests, `npx tsc --noEmit` and `npm run build` exit 0, and the built CLI exits 1 with `Error [CREDENTIALS_NOT_FOUND]` naming the path for both `CLAUDE_CONFIG_DIR=/nonexistent-dir` and `--config-dir /nonexistent-dir`. `npm run lint` still reports only the project's 12 pre-existing `tests/` errors — none in the files touched here. No make target or npm script added.

2026-08-17 13:22 - Configurable Claude config directory in the credentials adapter (step 1 of `claudeconfigdir-custom`). New `src/adapters/credentials/config-dir.ts` exposing `resolveConfigDir(explicit?)` — precedence explicit value -> `CLAUDE_CONFIG_DIR` -> `null`, with empty/whitespace-only values treated as unset at each level, trimming, and in-code `~/` and `~\` expansion via `os.homedir()` (env vars arrive unexpanded from config files and Docker). `readFromFile` in `src/adapters/credentials/file-reader.ts` now takes an optional config dir: when given, the single candidate is `<configDir>/.credentials.json` and the home/`%APPDATA%` list is skipped entirely; when omitted or `null` the existing candidate list is unchanged. `KeychainCredentialsProvider` takes an optional `CredentialsOptions` (new type in `src/core/types.ts`) as its only constructor argument, so `new KeychainCredentialsProvider()` keeps working; when a config dir resolves, it reads only that file and never touches the Keychain or the Windows Credential Manager, and a miss throws `CredentialsNotFoundError` naming the resolved path. No exports renamed or removed. Tests: new `tests/adapters/credentials/config-dir.spec.ts` (12 cases) plus config-dir cases added to `tests/adapters/credentials/file-reader.spec.ts` and `tests/adapters/keychain-credentials.spec.ts`; suite at 19 files / 149 tests green, `npx tsc --noEmit` and `npm run build` exit 0. `npm run lint` still reports the project's 12 pre-existing `tests/` errors (already filed in the ai-backlog) — none in the files touched here. The `--config-dir` CLI flag, the `getUsage()` option and the README/guidelines updates are step 2.

2026-08-16 11:20 - Tested the `totest/001-custom-exceptions` verification task: locked both asks with regression tests that the existing 109-test suite did not cover. Added `tests/errors.interception.spec.ts` (Ask 1 — drives the *real* `AnthropicUsageProvider` through a stubbed `fetch` while importing only the published entry `src/index.ts`, asserting 401/429/unparseable-body/network and missing-credentials all surface as typed `AiLimitsError` subclasses with their stable codes, that `UsageRequestError.status`/`statusText` are readable by a consumer, and that a client `switch (err.code)` routes all five codes to distinct branches with no fallback). Added `tests/shared/colored-output.spec.ts` (Ask 2 — a source-scan guard pinning `src/` to exactly the two known `console.error`/`console.warn` sinks so a new unpainted sink fails the build, plus red+bold error and yellow warning assertions under `FORCE_COLOR` and escape-code-free output under `NO_COLOR`). Added `tests/e2e/cli.e2e.spec.ts` — the user-level guard: compiles `src/` and runs the real `dist/cli.js` as a subprocess with piped (redirected) stderr, an empty fake `HOME` and a PATH-shadowed credential-store stub, so it is deterministic and offline; asserts exit 1, a real `Error [CREDENTIALS_NOT_FOUND]: <message>` on stderr, zero escape bytes when redirected, and ANSI red/bold under `FORCE_COLOR`. Mutation-checked all four guards (bare `Error` in the adapter, uncoloured `formatError`, a new unpainted `console.warn`, and colour leaking into redirected output) — each fails the intended spec and each source file was restored. Suite green at 18 files / 126 tests, no skips; `npm run build` exit 0; the new specs are lint-clean, leaving the project's 12 pre-existing `tests/` lint errors untouched (filed as `.docs/ai-backlog/2026-08-16_11-20-48-test-files-fail-lint.md`). No production code changed.

2026-08-16 09:22 - Added an ESLint 10 flat-config toolchain with type-aware `typescript-eslint`. New `eslint.config.js` (ESM) combining `@eslint/js` recommended + `recommendedTypeChecked`, driven by a new lint-only TS project `tsconfig.eslint.json` covering `src`, `tests` and root config files; `no-floating-promises` and `no-explicit-any` enforced as errors, the unsafe-* family relaxed for `tests/**/*.spec.ts`, and `dist`/`coverage`/`node_modules`/`.docs` ignored. Added `lint`/`lint:fix` npm scripts, a new `make/lint.mk` (`lint`, `lint-fix`) and `.eslintcache` to `.gitignore`. Toolchain only — the 12 problems it reports (including the floating promise in `src/cli.ts`) are fixed in a later step; `tsc --noEmit`, `npm test` and `npm run build` all still pass.

2026-08-16 09:13 - Specification for expanding the Makefile surface: produced a 4-step PRD — (1) new `make/test.mk` exposing the Vitest suite (`test`, `test-watch`, `test-cov`, `test-file`, `test-build`, `cov-open`), (2) `make/npm.mk` extended with dependency-maintenance and local-execution targets (`deps-audit`, `deps-fresh`, `link`/`unlink`, `install-global`/`uninstall-global`, `exec-latest`, `npm-info`, `npm-whoami`), (3) new `make/release.mk` taking over `pack`/`publish`/`version-*` and adding a guarded pipeline (`preflight`, `release-patch|minor|major`, `publish-tag`, `dist-tags`, `tag-push`), (4) documentation only — Makefile header, README and a new `.docs/guidelines/make-targets.md`. Steps 1–3 in `.docs/tasks/todo/`, step 4 in `.docs/tasks/document/`. Also filed an ai-backlog finding: `remove.npmrc` is tracked by git and contains a live npm publish token. No code implemented.

2026-08-16 09:06 - Specification for the orphaned `.docs/makefiles/` folder: decided `make/` is the single makefile source of truth (`.docs` is gitignored, so modules there are unversioned) and produced a 3-step PRD in `.docs/tasks/todo/` — (1) add `-include .env`, colour vars and `log_*` helpers to the root `Makefile`, (2) adapt and relocate `sonarqube.mk` into `make/` (`SONAR_SOURCES := src`, project key `ailimits`) so `make sonar-scan-quiet` resolves, (3) delete `.docs/makefiles/` and repoint the `makefile-setup` skill at `make/`. No code implemented.

2026-08-16 09:03 - Specification step for the missing lint/formatter toolchain: produced a 4-step PRD (ESLint flat config + typescript-eslint, Prettier, first-run fallout cleanup, and documentation/periodic-task realignment) under .docs/tasks/todo and .docs/tasks/document. No code changed.

2026-08-14 00:17 - Documented step 6 of the unit-test suite plan (testable CLI seam + entry point and library-facade tests). Added `.docs/guidelines/entry-points.md`, the module guideline for the app's two consumption paths: the CLI seams in `src/cli.ts` (`selectRenderer`, `formatError`, `run(argv)` returning the exit code, and the `isEntryPoint()`-guarded tail that keeps the import side-effect-free while `dist/cli.js` still runs as the `bin` entry with its shebang), and the library facade `src/index.ts` (`getUsage()` plus the re-exported building blocks, errors propagating unlogged). Each entry names its covering tests (`tests/cli.spec.ts`, `tests/cli.entry.spec.ts`, `tests/index.spec.ts`). Updated the existing "CLI seam unit tests" entry in `.docs/guidelines/testing.md` in place to list `tests/cli.entry.spec.ts` and describe the subprocess check it adds. Documentation only — no code changes.

2026-08-14 00:14 - Tested step 6 of the unit-test suite plan (testable CLI seam + entry point and library-facade tests). Reviewed `tests/cli.spec.ts` and `tests/index.spec.ts` against the acceptance criteria: the `selectRenderer` flag matrix, the three `formatError` cases, the three `run` paths (0 on success with stdout written, 1 with the error code on `console.error` for credential and usage failures), the side-effect-free import (no `process.exit`, no credential read, no fetch) and the public-surface checks are all present and green. Closed the one real gap: every unit test imports `src/cli.ts`, where the entry-point guard is false by construction, so a regression in `isEntryPoint()` would make `ailimits` exit 0 printing nothing with the whole suite still passing (line 59 was the only uncovered line). Added `tests/cli.entry.spec.ts`, which spawns the built `dist/cli.js` as a real process: asserts line 1 is still `#!/usr/bin/env node`, that a bare invocation produces non-empty output and exits 0 or 1, and that `--json` emits parseable JSON on stdout (or a formatted `Error` on stderr when there are no credentials / no network), so it is deterministic offline. Mutation-checked it — disabling the guard in `dist/cli.js` fails 2 of its 3 tests; the build was restored. `npm test` green (15 files / 109 tests, no skips), `npm run build` exit 0, coverage `src/cli.ts` 90.47% and `src/index.ts` 100% statements. No production code changed.

2026-08-14 00:09 - Validated step 6 of the unit-test suite plan (testable CLI seam + entry point and library-facade tests). Executable evidence: `npm test` green (14 files / 106 tests), `tests/cli.spec.ts` + `tests/index.spec.ts` 20 tests all passing in verbose mode, `npm run build` exit 0 with `dist/cli.js` line 1 = `#!/usr/bin/env node`, `node dist/cli.js` and `node dist/cli.js --json` both exit 0 printing real usage values (81% / 29%, no placeholders), and importing `dist/cli.js` executes nothing while exposing only `selectRenderer`, `formatError`, `run`. Coverage via json-summary: `src/cli.ts` 90.47% statements, `src/index.ts` 100% statements (both >= 90%). Applied one inline test fix: `tests/cli.spec.ts` now also asserts the import performs no credential read and no usage fetch, not just no `process.exit`. Validation PASSED.

2026-08-14 00:10 - Documented step 6 (fix step 2) of the unit-test suite plan in `.docs/guidelines/testing.md`: added a "Library facade unit tests" entry covering `tests/index.spec.ts` — the hoisted `vi.mock` doubles for both adapter modules (needed because `getUsage()` constructs its adapters internally), the identity assertions on the resolved payload and forwarded `accessToken`, the by-identity failure propagation with `console.error`/`process.exit` asserted never called, and the public-surface plus side-effect-free-import checks. Also updated the Vitest harness entry to note that `src/index.ts` is no longer excluded from coverage. Documentation only — no code changes.

2026-08-13 23:58 - Documented step 6 (fix step 1) of the unit-test suite plan in `.docs/guidelines/testing.md`: added a "CLI seam unit tests" entry covering `tests/cli.spec.ts` — the `importOriginal` mock of `../src/index.js` that keeps `UsageApp`, the renderers and the error classes real while stubbing only the two adapters, the `selectRenderer` flag matrix, the `formatError` code-block/plain-Error/non-Error cases asserted with `toContain`/`toMatch` so ANSI colours stay harmless, the three `run` paths (0 with `console.log`, 1 with the error `code` on `console.error` for credential and usage failures), and the file-wide `process.exit` spy plus entry-point-detection tests that pin the side-effect-free import. Documentation only — no code changes.

2026-08-11 02:52 - Tested step 6 (fix step 2) of the unit-test suite plan: verified the library facade `src/index.ts` is now measured and covered. `'src/index.ts'` is gone from `coverage.exclude` in `vitest.config.ts` (the file no longer contains the string) and the facade appears in `coverage/lcov.info` at 2/2 lines and 1/1 function — 100% statements against the required >= 90%; the v8 text table hides the row only because fully-covered files are collapsed by the reporter, and the `src` aggregate (91.3%) counts it. `tests/index.spec.ts` mocks both adapter modules with hoisted `vi.mock` factories, so no keychain read and no `fetch` occur: `getUsage()` is asserted to resolve the identical object returned by the mocked `fetchUsage`, to forward the exact `accessToken` from `getCredentials`, and to propagate a credentials failure and a usage failure by identity (`rejects.toBe`) with the `console.error` and `process.exit` spies asserted not called; the public-surface test checks all thirteen documented exports are defined, and a load-time snapshot of the mock call counts pins the side-effect-free import contract. Mutation-checked the guards — swapping the forwarded token for a literal and adding a top-level `new KeychainCredentialsProvider()` each fail the spec; `src/index.ts` was restored afterwards. `npm test` is green (14 files, 105 tests, no skips, also under `--sequence.shuffle`), `npm run build` exits 0, `dist/cli.js` line 1 is `#!/usr/bin/env node`, and both `node dist/cli.js` and `node dist/cli.js --json` print real usage values and exit 0. No production code changed (`git diff --name-only` is empty).

2026-08-10 23:49 - Documented step 5 of the unit-test suite plan (presentation and colour) in `.docs/guidelines/testing.md`: added a "Presentation and colour unit tests" entry covering the load-time palette and the env + `vi.resetModules()` + dynamic-import technique that forces the plain-text and coloured specs into separate files, the per-test `console.log` spy, the `JsonRenderer` single-line/round-trip contract, the `PrettyRenderer` assertions (fixed window order, skipped absent/null windows, 20-glyph bar with clamping, rounded padded percentage, timezone-independent reset line, extra-usage branches), and the 50%/80% threshold colour pins plus the `colors.ts` NO_COLOR/FORCE_COLOR/TERM=dumb/non-TTY rules. Documentation only — no code changes.

2026-08-10 22:24 - Tested step 6 (fix step 1) of the unit-test suite plan: added `tests/cli.spec.ts` covering the CLI seams. `selectRenderer` is asserted for `[]`, `['--json']`, `['--json','extra']` and `['--nope']` via `instanceof PrettyRenderer`/`JsonRenderer`; `formatError` is asserted with substring matching (ANSI-stripped, so TTY colour detection cannot make it brittle) for an `AiLimitsError` subclass (`Error [`, `CREDENTIALS_NOT_FOUND`, message), a plain `Error` (no `[` code block) and a non-Error thrown value; `run` is covered on three paths — success resolving `0` with `console.log` called, a rejecting credentials provider resolving `1` with `console.error` receiving `CREDENTIALS_NOT_FOUND` and `fetchUsage` never reached, and a rejecting usage provider resolving `1` with `NETWORK_ERROR`. `vi.mock('../src/index.js', importOriginal)` keeps `UsageApp`, the renderers and the error classes real and stubs only `KeychainCredentialsProvider` and `AnthropicUsageProvider`, so no keychain read and no `fetch` occur. A hoisted `vi.spyOn(process, 'exit')` guard is installed before the module import and re-installed after each `restoreAllMocks()`; it is asserted never called, including across re-imports of the module with `process.argv[1]` pointing at another file, at a non-existent path and at an empty string — pinning the `isEntryPoint()` side-effect-free import contract. `npm test` is green (13 files, 99 tests, no skips, also under `--sequence.shuffle`) and `src/cli.ts` reports 90.47% statements against the required >= 90%. No production code changed (`git diff --name-only` is empty).

2026-08-10 22:17 - Documented step 4 of the unit-test suite plan (core orchestration and error hierarchy) in `.docs/guidelines/testing.md`: added a "Core orchestration and error contract unit tests" entry covering the port-only `vi.fn()` stubs for `UsageApp`, the call-count/argument/ordering assertions, the fake-timer expiry boundary with the spied `console.warn` asserted on the 'looks expired' substring, the failure-propagation cases, and the `ErrorCode` contract pinning plus per-class `instanceof`/`code`/`name`/`message`/`cause` and `UsageRequestError` `status`/`statusText` checks. Documentation only — no code changes.

2026-08-10 22:11 - Tested step 5 of the unit-test suite plan (presentation layer). Added `tests/presentation/console-renderer.spec.ts` (colour disabled via `NO_COLOR` + `vi.resetModules()` and a dynamic import, `console.log` spied per test) covering the `JsonRenderer` single-line contract and its `JSON.parse` round-trip, the heading, the fixed window order regardless of payload key order, skipping of absent and `null`-utilization windows, bar rendering at 0/50/100/>100 with a constant 20-glyph width, percentage rounding and 3-column padding, the `resets_at` line derived from `Date` (no hard-coded locale literal) and the `no reset scheduled` branch, and the extra-usage block (disabled/absent, limit with currency, `n/a` limit, missing credits as `0`). Added `tests/presentation/console-renderer.thresholds.spec.ts` (`FORCE_COLOR=1`) pinning the 49→green / 50,79→yellow / 80,100→red thresholds, and `tests/shared/colors.spec.ts` covering `NO_COLOR`, `FORCE_COLOR=1` and `FORCE_COLOR=0`, `TERM=dumb`, non-TTY and TTY streams. `npm test` is green (12 files, 85 tests, no skips); `console-renderer.ts` reports 100% statements/lines and `colors.ts` 100% statements, lines and branches. Mutation-checked the guards — raising `WARN_AT`/`DANGER_AT` fails the threshold spec and reordering `WINDOW_LABELS` fails the ordering test; the source file was restored afterwards. No production code changed.

2026-08-10 22:02 - Re-verified step 4 of the unit-test suite plan after the interrupted test run: `npm run test:cov` is green (9 files, 59 tests, no skips) and `src/core/app.ts` and `src/core/errors.ts` both report 100% statement, branch and function coverage. Mutation-checked the guards — renaming `ErrorCode.NetworkError`'s value and flipping the `expiresAt < Date.now()` comparison each fail `tests/core` (4 failures); both source files were restored afterwards. No production code changed.

2026-08-10 20:45 - Tested step 4 of the unit-test suite plan (core orchestration and error hierarchy). Added `tests/core/app.spec.ts` covering `UsageApp.run()` against hand-written `vi.fn()` port fakes (no adapters imported): the happy path (each port called once, `fetchUsage` receiving the exact `accessToken`, `render` receiving the identical object returned by `fetchUsage`), the `getCredentials` → `fetchUsage` → `render` invocation order, the expired-token branch (one `console.warn` containing 'looks expired' while the flow still fetches and renders), the non-expired and absent-`expiresAt` branches (no warning), and failure propagation (a rejecting `getCredentials` skips both `fetchUsage` and `render`; a rejecting `fetchUsage` skips `render`), with `vi.useFakeTimers()`/`vi.setSystemTime()` pinning the expiry boundary and `console.warn` spied and restored so no stray warnings reach the output. Added `tests/core/errors.spec.ts` pinning the public `ErrorCode` contract with a whole-object comparison and checking each concrete class via a table: `instanceof AiLimitsError`/`Error`, its `code`, a `name` equal to its class name, a preserved `message` and a preserved `cause`, plus `UsageRequestError`'s `status`/`statusText`. `npm test` is green (9 files, 59 tests) and `src/core/app.ts` (8/8 statements, 4/4 branches) and `src/core/errors.ts" (11/11 statements) both report 100% coverage; renaming an `ErrorCode` value was verified to fail the spec.

2026-08-10 19:05 - Documented step 3 of the unit-test suite plan (Anthropic usage API client) in `.docs/guidelines/testing.md`: added a "Usage API client unit tests" entry covering the `globalThis.fetch` `vi.fn()` swap and restore, the plain-object fake responses cast to `Response` (so `json()` can reject), the full request-shape assertions (exact URL, `GET`, whole headers object, one call per `fetchUsage`) and the error mapping by code (401 → `AuthenticationError`, 500/429 → `UsageRequestError`, bad JSON → `UsageResponseError`, thrown `fetch` → `NetworkError` with `cause`). Documentation only — no code changes.

2026-08-10 17:50 - Tested step 3 of the unit-test suite plan (Anthropic usage API client). Added `tests/adapters/anthropic-usage.spec.ts` covering `AnthropicUsageProvider.fetchUsage`: the exact request shape (URL `https://api.anthropic.com/api/oauth/usage`, `method: 'GET'` and a full-object comparison of the five headers, so adding or renaming one fails the test), the HTTP 200 success path resolving to the parsed payload, and every failure mapping — 401 → `AuthenticationError` (`AUTHENTICATION_FAILED`), 500/429 → `UsageRequestError` exposing `status`/`statusText` (`USAGE_REQUEST_FAILED`), a rejecting `json()` → `UsageResponseError` (`USAGE_RESPONSE_INVALID`), and a thrown `fetch` → `NetworkError` (`NETWORK_ERROR`) preserving `error.cause`; each rejection is asserted to be an `AiLimitsError` with a non-empty message and a `name` equal to its class. `globalThis.fetch` is replaced with a `vi.fn()` in `beforeEach` and restored in `afterEach`, fake responses are plain objects cast to `Response` so `json()` can reject, and the call count is asserted — no outbound request is made. `npm test` is green (7 files, 35 tests) and `src/adapters/anthropic-usage.ts` reports 100% statement (11/11) and 100% branch (4/4) coverage.

2026-08-10 16:55 - Documented step 2 of the unit-test suite plan (credential reading) in `.docs/guidelines/testing.md`: added a "Credential reading unit tests" entry listing the five spec files, the hoisted `vi.mock` of `node:child_process`/`node:fs/promises` with the `promisify.custom` `execFile` mock, the per-test `process.platform` override/restore that keeps the suite order-independent, and the provider routing/fallback cases asserted. Documentation only — no code changes.

2026-08-10 15:59 - Tested step 2 of the unit-test suite plan (credential reading). Added five spec files — `tests/adapters/credentials/parse.spec.ts`, `file-reader.spec.ts`, `keychain-reader.spec.ts`, `wincred-reader.spec.ts` and `tests/adapters/keychain-credentials.spec.ts` — covering blob parsing (BOM strip, empty, missing `claudeAiOauth`/`accessToken`, invalid JSON throw), the file candidate order (home-only on non-win32, `%APPDATA%` fallback on win32, APPDATA unset, all-miss → null), the macOS `security` invocation and its args, the `powershell.exe -EncodedCommand` invocation whose base64/utf16le script contains `Claude Code-credentials`, and the provider resolution order per platform including the `CredentialsNotFoundError` (`CREDENTIALS_NOT_FOUND`) case and the pass-through of expired credentials. `node:child_process` (via a `promisify.custom` mock), `node:fs/promises` and `node:os` are mocked, so no process is spawned and no file is read; `process.platform` is overridden per test and restored in `afterEach`. `npm test` is green (6 files, 25 tests), passes under `--sequence.shuffle`, and all five target source files report 100% line coverage in `coverage/lcov.info`.

2026-08-07 15:06 - Specified the fix for step 6 of the unit-test plan (test half only). Split into two independent PRD step files under `.docs/tasks/test/`: step 1 covers `tests/cli.spec.ts` (`selectRenderer`, `formatError`, the three `run` paths with mocked adapters, and a `process.exit` guard asserting import is side-effect-free); step 2 covers removing `src/index.ts` from `coverage.exclude` in `vitest.config.ts` plus `tests/index.spec.ts` (`getUsage()` happy path, error propagation, public-surface assertions). No production code changes are in scope. Source brief moved to `.docs/tasks/done/2026-08-07_09-25-51_unit-tests-main-features.step-6.fix.specified.md`.

2026-08-07 15:02 - Validated step 6 of the unit-test suite plan — FAILED. The production seams in `src/cli.ts` are correct and verified end-to-end (`npm run build` exits 0, shebang intact, `node dist/cli.js` and `node dist/cli.js --json` print real values and exit 0, importing `dist/cli.js` executes nothing and exposes `selectRenderer`/`formatError`/`run`), but the required test files `tests/cli.spec.ts` and `tests/index.spec.ts` were never created: `npm test` runs only `smoke.spec.ts`, `src/cli.ts` sits at 0% statement coverage and `src/index.ts` is excluded from coverage in `vitest.config.ts`. Fix brief written to `.docs/tasks/specification/2026-08-07_09-25-51_unit-tests-main-features.step-6.fix.md`.

2026-08-07 14:53 - Made the CLI unit-testable by extracting seams in `src/cli.ts`: exported `selectRenderer(argv)`, a pure `formatError(err)` and `run(argv)` returning the exit code instead of calling `process.exit`. The side-effectful tail is now guarded by an entry-point check (realpath of `process.argv[1]` vs `import.meta.url`), so importing the module in tests performs no credential read, no fetch and no exit. Observable CLI behaviour, output text, exit codes and the shebang are unchanged.

# Changelog

2026-08-07 10:47 - Evaluated step 1 of the unit-test suite plan (vitest harness). Re-ran and confirmed all acceptance criteria: `npm test` exits 0 with 1 passing test, `npm run test:cov` exits 0, prints the coverage table (rows for `src/**`) and writes `coverage/lcov.info`, `npm run build` exits 0 with no `*.spec.js` or `tests` directory under `dist/`, and `git status --porcelain` is clean after the coverage run. This step is infrastructure-only (no application behavior to test), so no additional tests or fix tasks were needed.

2026-08-07 10:35 - Implemented step 1 of the unit-test suite plan: wired up a vitest test harness. Added `vitest` and `@vitest/coverage-v8` as devDependencies, added `vitest.config.ts` (node environment, `tests/**/*.spec.ts` glob, v8 coverage over `src/**/*.ts` excluding `src/index.ts`), and added `npm test` / `npm run test:watch` / `npm run test:cov` scripts to `package.json`. Added `tests/smoke.spec.ts`, which imports `ErrorCode` from `src/core/errors.js` with the `.js` specifier to prove ESM + TS resolution works under vitest. Added `coverage` to `.gitignore`. Verified `npm test`, `npm run test:cov` (writes `coverage/lcov.info`), and `npm run build` (unchanged `dist/`, no test files) all pass.

2026-08-07 09:25 - Specified the unit-test suite for the main `ailimits` features as six PRD step files. Step 1 (todo) wires a vitest harness with `npm test` / `npm run test:cov`; steps 2–5 (test) cover credential reading across macOS Keychain, Windows Credential Manager and the JSON file fallback, the Anthropic usage API client with a stubbed `fetch`, `UsageApp` orchestration plus the `ErrorCode` contract, and the pretty/JSON renderers with their colour thresholds; step 6 (todo) adds small seams to `src/cli.ts` (`selectRenderer`, `formatError`, `run` returning an exit code) so flag parsing and exit codes become testable, and covers the `getUsage()` facade. Source brief moved to `.docs/tasks/done/`. No production or test code was written.

2026-05-21 - Fixed `npm publish` warning by removing the leading `./` from `bin.ailimits` in `package.json` (`./dist/cli.js` → `dist/cli.js`). npm's publish-time normaliser rejects relative-prefixed paths in the `bin` map (it accepts them in `main`/`exports`), and was silently stripping the entry, which would have shipped the package without an executable. Ran `npm pkg fix` to apply the correction. Also diagnosed the accompanying 404 on publish as an authentication failure (`npm whoami` returns 401) rather than a registry issue — the package name is owned by the same npm user, so `npm login` followed by `npm publish` will succeed.

2026-05-21 - Added Windows support for the credentials reader. `KeychainCredentialsProvider` now resolves the Claude Code OAuth token from the Windows Credential Manager (Generic credential, target `Claude Code-credentials`) on `win32`, in addition to the existing macOS Keychain path; both platforms still fall back to the JSON file. The Windows reader shells out to `powershell.exe` with a base64-encoded script that P/Invokes `advapi32!CredReadW` and decodes the UTF-16 LE blob — no native dependencies are introduced. Refactored the credentials adapter into per-strategy files under `src/adapters/credentials/` (`keychain-reader.ts`, `wincred-reader.ts`, `file-reader.ts`, shared `parse.ts`) following the project's SOLID / hexagonal layout. The file reader also probes `%APPDATA%\\.claude\\.credentials.json` on Windows and strips an optional UTF-8 BOM defensively.

2026-05-19 - Expanded the Make build system into grouped sub-makefiles. The root `Makefile` now auto-includes every `make/*.mk` file and renders a self-documenting, colourised `help` from `## description` annotations grouped under `##@` section headers. Added `make/build.mk` (`build`, `dev`, `watch`, `typecheck`, `clean`, `rebuild`), `make/run.mk` (`run`, `run-json`, `start`) and `make/npm.mk` (`install`, `deps-update`, `deps-outdated`, `pack`, `publish-check`, `version-patch/minor/major`, `publish`). Updated `make/claude.mk` to the shared annotation style and fixed its malformed `.PHONY` declaration.

2026-05-18 22:16 - Added a typed custom error hierarchy and colourised CLI output. New `core/errors.ts` defines `AiLimitsError` (base) plus `CredentialsNotFoundError`, `AuthenticationError`, `UsageRequestError`, `UsageResponseError` and `NetworkError`, each carrying a stable machine-readable `code` (`ErrorCode`) for client apps to intercept. The adapters now throw these instead of plain `Error`. Added `shared/colors.ts`, a TTY/`NO_COLOR`/`FORCE_COLOR`-aware ANSI palette: the CLI prints errors in red (with their code) and the expired-token warning in yellow, and the pretty renderer colour-codes usage bars by severity (green/yellow/red). All error types are exported from the library entry point.

2026-05-18 14:45 - Added a publishing guideline at `.docs/guidelines/publishing-to-npm.md` covering how to release the project to npm as both an importable dependency and an installable CLI (setup, pre-publish checklist, versioning, publish steps, safety notes).

2026-05-18 14:35 - Made the package usable both as a CLI and as a library dependency. Added a side-effect-free library entry point `src/index.ts` (re-exports plus a `getUsage()` convenience function); separated it from the `bin` CLI entry `cli.ts`, which now consumes the public API. Updated `package.json` (`main`/`types`/`exports`/`sideEffects`) and enabled TypeScript declaration output (`tsconfig.json`).

2026-05-18 14:00 - Converted the project from JavaScript to TypeScript and reorganised it into a hexagonal architecture: `core/` (domain types, ports, `UsageApp` use case), `adapters/` (Keychain credentials + Anthropic usage API), `presentation/` (pretty and JSON renderers) and `cli.ts` as the composition root. Added `tsconfig.json`, build/dev scripts and TypeScript dev dependencies.
2026-08-16 10:51 - Validated the ESLint flat-config toolchain (step-1) end-to-end: clean `npm ci`, ESLint v10.8.1, type-aware linting confirmed live by a `no-floating-promises` error in `src/cli.ts:59`, ignore patterns, typecheck, 109 tests, build output and `make help` all verified. AC8's "same exit code" clause was proven unsatisfiable (GNU make maps any recipe failure to exit 2); logged as a backlog wording fix.

2026-08-16 11:00 - Test step for the ESLint flat-config toolchain (step-1): skipped as not applicable. The task ships only lint tooling (`eslint.config.js`, `tsconfig.eslint.json`, `make/lint.mk`, the `lint`/`lint:fix` scripts, `.gitignore`) with source and tests explicitly out of scope, so there is no runtime, API or UI surface to regress. Its acceptance criteria are themselves shell commands already executed against the real repo in the do and validate runs; wrapping them in a spec would only assert that config files exist. No test files added or changed, no defect found, no fix task created.

2026-08-16 11:11 - Validated the parked `totest/001-custom-exceptions.md` asks as already implemented, then retired the file. Ask 1 proven executably: every failure path of the compiled `AnthropicUsageProvider` (network throw, 401, 429, 500, unparseable body) throws an `AiLimitsError` subclass with its stable `code` and a preserved `cause`, and a real consumer package installed from this repo intercepted a live HTTP 429 by `err.code`/`instanceof` through the public entry point. `CredentialsNotFoundError` exercised via the real CLI with no OS store and an empty home. Ask 2 proven: the only two warn/error sites in `src/` are both coloured — the expired-token warning yellow (`\e[33m`) and the CLI error red+bold (`\e[31m\e[1m`) — redirected and `NO_COLOR` output carry zero escape bytes while a TTY and `FORCE_COLOR=1` carry them, and the pretty renderer tints 12/63/91% green/yellow/red. Moved `.docs/tasks/totest/001-custom-exceptions.md` to `.docs/tasks/done/`, leaving the legacy `totest/` queue empty. No source changes.
