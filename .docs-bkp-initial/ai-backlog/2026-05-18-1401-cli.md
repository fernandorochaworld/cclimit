# Add automated tests

The project has no test suite. The core logic (window formatting, progress
bar rendering, credential parsing) is pure and easy to unit test.

Suggestion: add a test runner (Node's built-in `node:test` keeps it
dependency-light) covering `console-renderer.ts` and the `parseOauth`
helper, plus a smoke test that wires fake adapters into `UsageApp`.
